import type { Constraint } from "../domain/types";
import type { StoreSnapshot } from "../domain/store";
export type ConstraintCallbacks=Readonly<{update:(id:string,value:Constraint)=>void;remove:(id:string)=>void}>;
export function constraintsList(snapshot:StoreSnapshot,callbacks:ConstraintCallbacks):HTMLElement{const section=document.createElement("section");section.className="constraints panel-section";section.dataset.constraintList="";section.setAttribute("aria-labelledby","constraints-heading");const heading=document.createElement("h2");heading.id="constraints-heading";heading.textContent="Constraints";section.append(heading);const help=document.createElement("p");help.className="section-help";help.textContent="Required constraints must pass; preferred constraints guide ranking.";section.append(help);if(snapshot.workingState.constraints.length===0){section.append(Object.assign(document.createElement("p"),{textContent:"No constraints in this layout."}))}
for(const constraint of snapshot.workingState.constraints){const form=document.createElement("form");form.className="constraint-row";form.dataset.constraintId=constraint.constraintId;form.dataset.constraintRow="";const title=document.createElement("h3");title.textContent=`${constraint.constraintId} · ${constraint.type.replaceAll("_"," ")} · ${constraint.strength} · ${constraint.type==="door_path_clear"?constraint.widthMm:constraint.thresholdMm} mm`;form.append(title);const strengthLabel=document.createElement("label");strengthLabel.textContent="Strength";const strength=document.createElement("select");strength.disabled=!!snapshot.preview;strength.setAttribute("aria-label","Strength");for(const v of ["required","preferred"]){strength.append(Object.assign(document.createElement("option"),{value:v,textContent:v[0].toUpperCase()+v.slice(1)}))}strength.value=constraint.strength;strengthLabel.append(strength);form.append(strengthLabel);const valueLabel=document.createElement("label");const value=document.createElement("input");value.type="number";value.step="1";value.disabled=!!snapshot.preview;if(constraint.type==="door_path_clear"){valueLabel.textContent="Clear width (mm)";value.min="500";value.max="1600";value.value=String(constraint.widthMm)}else{valueLabel.textContent="Distance threshold (mm)";value.min="0";value.max="4000";value.value=String(constraint.thresholdMm)}value.setAttribute("aria-label",valueLabel.textContent);valueLabel.append(value);form.append(valueLabel);const distanceConstraint=constraint.type==="door_path_clear"?null:constraint;const relation=distanceConstraint?document.createElement("select"):null;if(relation&&distanceConstraint){const relationLabel=document.createElement("label");relationLabel.textContent="Relation";relation.append(Object.assign(document.createElement("option"),{value:"near",textContent:"Keep near"}),Object.assign(document.createElement("option"),{value:"away",textContent:"Keep away"}));relation.value=distanceConstraint.relation;relation.disabled=!!snapshot.preview;relationLabel.append(relation);form.append(relationLabel)}const update=document.createElement("button");update.type="submit";update.textContent="Update constraint";update.disabled=!!snapshot.preview;const remove=document.createElement("button");remove.type="button";remove.textContent="Delete constraint";remove.disabled=!!snapshot.preview;remove.dataset.deleteConstraint=constraint.constraintId;remove.addEventListener("click",()=>callbacks.remove(constraint.constraintId));form.append(update,remove);form.addEventListener("submit",event=>{event.preventDefault();const amount=Number(value.value);const base={...constraint,strength:strength.value as Constraint["strength"]};const next=constraint.type==="door_path_clear"?{...base,widthMm:amount}:{...base,relation:relation!.value as "near"|"away",thresholdMm:amount};callbacks.update(constraint.constraintId,next as Constraint)});section.append(form)}return section}
